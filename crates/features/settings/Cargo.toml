[package]
name = "nai-atelier-settings"
version = "0.1.0"
edition = "2024"
publish.workspace = true

[dependencies]
async-trait = "0.1"
nai-atelier-generation = { path = "../generation" }
thiserror = "2"

[dev-dependencies]
futures-executor = "0.3"
